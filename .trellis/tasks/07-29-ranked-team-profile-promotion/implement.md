# 实施计划

## 1. 协议与纯逻辑

- [x] 扩展 `RoomMode`、创建房间输入、组队匹配投影和排队状态。
- [x] 更新协议解析/类型测试与小程序投影兼容层。
- [x] 扩展匹配候选模型，支持不可拆分的 2–4 人队伍组合。
- [x] 为 `1+1+1+1`、`2+1+1`、`2+2`、`3+1`、`4` 及禁止拆队添加算法测试。

## 2. 数据库与服务端

- [x] 为排队表添加 `party_id/party_size` 幂等迁移、索引和行映射。
- [x] 增加队伍批量入队、查询、整队取消与断线过期事务。
- [x] 在 `MatchmakingService` 中实现队伍入队、恢复、取消和调度。
- [x] 在 `RoomService` 中实现 `TEAM_MATCH` 创建、加入、准备与队伍状态投影。
- [x] 增加开始/取消组队匹配接口，并协调离房、解散与房主转移。
- [x] 匹配成功后复用现有 `MATCH` 房间、竞技结算和确认流程。
- [x] 增加数据库、匹配算法、匹配服务、房间服务与协议关键测试。

## 3. 小程序功能

- [x] 新增 8 个段位徽章 SVG 资产与 `RankBadge` 组件。
- [x] 主页账号头像接入统一玩家详情弹窗。
- [x] 首页拆分单人排位与组队排位入口，并支持组队恢复。
- [x] 组队等待牌桌增加人数/准备/房主开始/全员取消状态。
- [x] 更新 `useRoom` 与 HTTP API 以支持开始、取消组队匹配。
- [x] 玩家详情、主页、排队状态与结算统一使用段位徽章 + 文字。
- [x] 新增 `RankPromotionOverlay`，接入结算并防止重复播放。
- [x] 添加触发条件、投影恢复和交互状态的纯逻辑测试。

## 4. 验证门禁

- [x] 运行协议与游戏引擎定向测试。
- [x] 运行服务端数据库、匹配算法、匹配服务、房间服务定向测试。
- [x] 运行小程序单元测试、类型检查和构建。
- [x] 按 `trellis-check` 完成规格、数据流、复用、lint、typecheck、test、build 检查。
- [ ] 人工核对横屏小程序：首页双入口、头像详情、2/3/4 人组队、取消重准备、徽章层级与晋升动画。

## 5. 回滚点

- 协议/数据库阶段：新增列为可空，可回退代码。
- 服务端阶段：组队入口未接 UI 前不影响现有单排。
- 小程序阶段：可单独隐藏组队入口或退回纯文字段位，不影响竞技结算。
